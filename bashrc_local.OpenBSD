# ----------------------------------------------------------------------
#  $Id$
#
#  Addenda to ~/.bashrc specific to machine/site
#  (OpenBSD stuff)
# ----------------------------------------------------------------------

# Fuzzy finder integration
[ -f /usr/local/share/fzf/bash/key-bindings.bash ] &&
        source /usr/local/share/fzf/bash/key-bindings.bash
[ -f /usr/local/share/fzf/bash/completion.bash ] &&
        source /usr/local/share/fzf/bash/completion.bash
